import Jumbotron from 'react-bootstrap/Jumbotron';

import './Header.css';

const Header = () => {
    return (
        <header>
            <Jumbotron>
                <h1 className="header">In My Cupboard...</h1>
            </Jumbotron>
        </header>

    );
};

export default Header;
